This zip contains only changed/new files for Route-2: stateful L2 augmentation (velocity/agg/graph) + /debug/backend endpoint parity.

Apply by extracting at repo root (monster-risk) and overwriting paths.

Files are full-file replacements (not unified diff).
